---
description: A sample documentation set that ships with docuservice.
---

# Contoso Platform Docs

This folder is an ordinary set of Markdown files — the kind that already lives in
most Azure Repos. Nothing here is docuservice-specific except the optional
`docuservice.json` at the root.

Start with the [getting started guide](guides/getting-started.md), or jump to the
[configuration reference](reference/configuration.md).
